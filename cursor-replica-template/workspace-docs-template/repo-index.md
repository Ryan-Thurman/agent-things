# Repo Index

Use this as the fast lookup table for the workspace.

## Repos

- repo: `infra`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `etl`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `factory`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `service`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `learning`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `privacy`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `public_pages`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `tco`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `pwd`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:

- repo: `hcp`
  - purpose:
  - primary stack:
  - owned atlas:
  - main SDDs:
  - main SRS docs:
  - common neighboring repos:
