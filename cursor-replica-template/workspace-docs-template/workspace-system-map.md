# Workspace System Map

Keep this short. It is the cross-repo coordination map for the workspace, not a replacement for repo-local atlases.

For each item, prefer:

- `confidence: confirmed | inferred | unknown`
- `evidence:` file paths, config names, endpoints, queue names, or other concrete signals

Do not silently guess.

## Workspace Scope

- Repos in scope:
- Primary product or domain:
- Shared environments:
- Shared platforms or vendors:

## Repo Roles

- repo: `infra`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `etl`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `factory`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `service`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `learning`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `privacy`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `public_pages`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `tco`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `pwd`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

- repo: `hcp`
  - owns:
  - key contracts:
  - most common adjacent repos:
  - confidence:
  - evidence:

## Shared Contracts Across Repos

- contract:
  - owner repo:
  - consumer repos:
  - verification path:
  - confidence:
  - evidence:

## Common Coordinated Change Paths

- change type:
  - repos usually touched:
  - normal sequence:
  - main risk:
  - confidence:
  - evidence:

## Highest-Risk Boundaries

- boundary:
  - repos involved:
  - why it is risky:
  - how to verify safely:
  - confidence:
  - evidence:
