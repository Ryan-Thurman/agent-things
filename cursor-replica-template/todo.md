# Tasks

- [ ] Replace this line with the first pending task
- [ ] Add more tasks as needed

# Notes

- Use `[~]` for the single task currently in progress.
- Use `[x]` only when the task is actually complete.
